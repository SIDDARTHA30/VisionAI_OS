# Automation Module

This directory contains logic for triggering and executing automated workflows.

## Core Features
- Rules engine linking vision detection events (e.g., "motion detected") to actions.
- Action handlers (e.g., sending API requests, running terminal scripts, playing audio warnings).
- Task runner queue and scheduler.
