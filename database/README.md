# Database Layer

This directory manages database setup, schema definitions, migrations, and database seed data.

## Core Features
- Database clients and session pools.
- Initial migrations and schema setup scripts.
- Seed data for user profiles, default vision rules, and system configurations.
