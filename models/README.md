# Models

This directory contains definitions for data models, entities, types, and schemas used across different sub-modules.

## Structure
- Data structures/entities representing DB tables (ORM models).
- Schema validators (e.g., Pydantic models or TypeScript interfaces).
- DTO (Data Transfer Object) definitions for API request/response validation.
